const sortByCount = require('./sortByCount');
const splitByWords = require('./splitByWords')
const wordMap = require('./wordMap');
const filterData = require('./filterData');

module.exports = {
    sortByCount,
    splitByWords,
    wordMap,
    filterData
}